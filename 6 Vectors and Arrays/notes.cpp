#include<iostream>
#include<string>
#include<vector>

//vector of transactions is filled with the months transactions
//vector of doubles which empty initially
balance = 0.0
for (int day = 0; day < MAX_DAY; day++)
{
  balance = sum_total_for_day(day + 1)
  //push_back balance on vector
}


sum_total_for_day
total = 0
for (int i = 0; i < transactions.size(); i++)
{
  if (day == transactions[i].get_day)
  {
    total = total + transactions[i].get_amount

  }
  return total;
}
